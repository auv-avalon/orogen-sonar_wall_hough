# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(sonar_wall_houghTaskLib)
ADD_LIBRARY(${SONAR_WALL_HOUGH_TASKLIB_NAME} SHARED 
    ${SONAR_WALL_HOUGH_TASKLIB_SOURCES})

add_dependencies(${SONAR_WALL_HOUGH_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${SONAR_WALL_HOUGH_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${SONAR_WALL_HOUGH_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SONAR_WALL_HOUGH_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SONAR_WALL_HOUGH_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SONAR_WALL_HOUGH_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SONAR_WALL_HOUGH_TASKLIB_HEADERS} DebugTypes.hpp
    DESTINATION include/orocos/sonar_wall_hough)

